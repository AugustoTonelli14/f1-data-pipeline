name: f1_pipeline
version: "1.0.0"
config-version: 2

profile: f1_pipeline

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]

clean-targets:
  - target
  - dbt_packages

models:
  f1_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
